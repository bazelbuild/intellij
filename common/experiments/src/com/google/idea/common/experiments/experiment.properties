# File contains values for plugin experiment flags.

# Each line of the file except comment lines marked by # or ! symbols
# contains one flag. For each flag, the format `flag_name=value` is used.

# Flags used by ASwB
# Use Studio's deployer: b/197761450
aswb.use.studio.deployer.2=0

# [Rollout %] Enable Kotlin coroutines debugging
kotlin.coroutinesDebugging.enabled=100

# [Rollout %] Move minimum supported version of Bazel to 1.2.0
new.oldest.bazel.version.enabled=70