---
platforms:
  ubuntu1804:
    build_flags:
      - --define=ij_product=intellij-beta
    build_targets:
      - //aspect:aspect_files
    test_flags:
      - --define=ij_product=intellij-beta
      - --test_output=errors
      - --notrim_test_configuration
    test_targets:
      - //aspect/testing/...
