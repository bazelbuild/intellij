---
platforms:
  ubuntu1604:
    build_flags:
      - --define=ij_product=clion-beta
    build_targets:
      - //clwb/...
    test_flags:
      - --define=ij_product=clion-beta
      - --test_output=errors
    test_targets:
      - //:clwb_tests
  ubuntu1804:
    build_flags:
      - --define=ij_product=clion-beta
    build_targets:
      - //clwb/...
    test_flags:
      - --define=ij_product=clion-beta
      - --test_output=errors
    test_targets:
      - //:clwb_tests
